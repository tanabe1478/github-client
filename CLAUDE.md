# Claude Code project instructions

Read and follow `AGENTS.md`. For UI work, also load `.agents/skills/figma-design/SKILL.md` and use the project-scoped `figma` MCP server configured in `.mcp.json`.
